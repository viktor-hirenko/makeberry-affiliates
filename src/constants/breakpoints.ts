/**
 * JS-зеркало `$breakpoints` из `src/assets/styles/scss/_breakpoints.scss`.
 * При смене значений в SCSS — обновить и здесь.
 */

/** `$breakpoints.compact` — 2 колонки в сетках / слайдер */
export const BREAKPOINT_COMPACT_PX = 600

/** `$breakpoints.mobile` — padding 32px */
export const BREAKPOINT_MOBILE_PX = 768

/** `$breakpoints.narrow-tablet` — между mobile и tablet (контейнер ~1040) */
export const BREAKPOINT_NARROW_TABLET_PX = 960

/** `$breakpoints.tablet` — 3–4 колонки, focal carousel */
export const BREAKPOINT_TABLET_PX = 1024

/** `$breakpoints.laptop` — стрелки слайдеров, полный логотип */
export const BREAKPOINT_LAPTOP_PX = 1280

/** `$breakpoints.desktop` — max-width контейнера */
export const BREAKPOINT_DESKTOP_PX = 1440
